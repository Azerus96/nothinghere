// ════════════════════════════════════════════════════════════════════════
// bet_translation.cpp — Module 3.3 implementation
// (all core functions are inline in bet_translation.h; this translation
//  unit anchors the header for the library and documents the module)
// ════════════════════════════════════════════════════════════════════════
#include "bet_translation.h"

namespace postflop {
// pseudo_harmonic_distance, needs_local_expansion, nearest_grid_point and
// translate_observed_bet are defined inline in bet_translation.h per the
// specification (all are leaf, stateless, noexcept functions).
}
